import RegisterForm from "@/components/RegisterForm";

export default function RegisterStudentPage() {
  return <RegisterForm isTeacher={false} title="Create a student account" subtitle="Start learning English today." />;
}
